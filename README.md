### v10.5.114 普通问事渲染中断与顶部复位修正版

本版基于 v10.5.113 继续做主源码稳定修复，重点处理普通问事结果页偶发“内容缺失 / 混入上一条内容 / 从中间开始显示”的问题。

1. 修复 `buildSceneFocusHTML()` 调用旧 `escapeHtml` 名称导致的 `ReferenceError`。该错误会让普通问事结果页渲染到“本场景判断重心”时中断，造成后续模块没有刷新或残留上一条结果内容。
2. 增加全局兼容别名 `escapeHtml -> escapeHTML`，避免后续同类命名大小写错误再次中断渲染。
3. 结果页顶部复位提前到渲染开始，并在渲染完成后再次校准；重新占卜、历史记录查看、切换命运/非命运结果时，都优先从结果页顶部开始。
4. 普通问事切换时清空隐藏的“六宫排盘”和“命运六亲参考盘”残留 DOM；命运场景仍由主源码重新生成排盘、命盘和命盘二级导航。
5. 继续保留 v10.5.113 的普通问事精简结构：普通问事不显示排盘/命盘，命运场景保留排盘/命盘。
6. Debug 自检继续检查运行错误、结果页复位、普通问事/命运模板边界、导航 target、旧补丁残留等项目。
7. 继续保持 7 个完整文件交付，无后置补丁、无延迟抢写补丁。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 从历史记录打开普通问事记录，确认不再报 `ReferenceError: Can't find variable: escapeHtml`。
3. 普通问事结果页确认“本场景判断重心”“综合判断”“三象”“单宫”“双宫”“象意”“雷达”“建议”“术语”都能正常刷新。
4. 从命运记录切换回普通问事记录，确认普通问事不会混入命运文案，不显示“六宫排盘”和“命运六亲参考盘”。
5. 重新占卜和历史查看都应从结果页顶部开始显示。
6. 在 Debug 点“一键自检”，确认没有 `RUNTIME_ERRORS`、`RESULT_PAGE_SCROLL_NOT_RESET`、`NORMAL_TEMPLATE_SHOWING_PAIPAN`、`NORMAL_TEMPLATE_SHOWING_DESTINY_BOARD`。

### v10.5.113 普通问事排盘精简与自检增强版

本版基于 v10.5.112 继续做主源码精简，重点把普通问事结果页中的“六宫排盘”和“命运六亲参考盘”收回命运场景专用。

1. 普通问事（非命运场景）不再显示“六宫排盘”。
2. 普通问事继续隐藏“命运六亲参考盘”，避免历史回看或重新占卜时混入命运模板。
3. 命运场景仍保留“六宫排盘”和“命运六亲参考盘”，并继续保留命盘二级导航与各专题模块。
4. 普通问事主导航同步移除“排盘”。
5. Debug 自检新增普通问事排盘隐藏检查与命运排盘缺失检查。

### v10.5.112 结果页顶部复位与普通问事回归版

本版基于 v10.5.111 继续做主源码修复，重点解决结果页进入后没有稳定回到顶部的问题：

1. 新增 `window.XLRResultPageCore` 主源码命名空间，把结果页顶部复位、导航初始锁定和 Debug 状态读取统一收回主流程。
2. 修复重新占卜进入结果页时沿用旧滚动位置的问题：`renderResult()` 完成后强制回到 `resultCard` 顶部。
3. 修复从历史记录点“查看”进入结果页时可能从导读或中部开始显示的问题，进入结果页默认高亮“结果”。
4. 展开全部、折叠全部后同步执行顶部复位，避免 iPhone Safari 因内容高度变化自动锚定到中间。
5. Debug 新增结果页初始滚动位置检查，若刚进入结果页仍 `scrollY` 偏大，会报告 `RESULT_PAGE_SCROLL_NOT_RESET`。
6. 回归普通问事与命运模板边界：命运页继续隐藏单宫、双宫和普通行动建议；普通问事仍保留这些学习模块。
7. 继续保持 7 个完整文件交付，无后置补丁、无延迟抢写补丁。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 重新占卜一次普通问事，确认结果页从最顶部“占问场景 / 占卜结果”开始显示。
3. 重新占卜一次命运，确认结果页从顶部开始显示，主导航默认停在“结果”。
4. 从历史记录分别查看一条普通问事和一条命运记录，确认两者都从顶部开始显示。
5. 在结果页点击“展开”和“折叠”，确认页面不会停在导读或中部。
6. 在 Debug 点“一键自检”，确认没有 `RESULT_PAGE_SCROLL_NOT_RESET`，并且普通问事不串命盘模板、命运页不串普通问事模块。

### v10.5.111 命盘子导航顺序与暗夜自检修正版

本版基于 v10.5.110 继续做主源码修复，重点解决两个实测问题：

1. 修正命盘二级导航顺序与实际渲染顺序不一致的问题，特别是从“婚姻”切换到“成果”时 active 状态回跳、看起来切换失败的问题。
2. 补齐六宫排盘命中宫 `.v105107-paipan-cell.is-hit` 的暗夜主题 CSS 覆盖，消除 Debug 自检中的 `NIGHT_THEME_COVERAGE_MISSING` 警告。
3. 继续保持命运页精简结构：命运结果页不显示单宫断事、双宫断事和普通行动建议。
4. 继续保持 7 个完整文件交付，无后置补丁、无延迟抢写补丁。

### v10.5.110 命盘暗夜排盘与命运精简主源码版

- 直接在 v10.5.109 命盘总览布局与六宫排盘校正版上修改，继续保持 GitHub 可直接上传的 7 个完整文件。
- 修复暗夜模式下六宫排盘命中宫变成整块亮紫的问题：排盘命中状态改用专用 `is-hit` 类，不再被通用 `.active` 按钮/导航样式误伤。
- 补齐六宫排盘在暗夜模式下的主 CSS 覆盖：普通宫、命中宫、右上序号、落宫标签与“未落”标签都统一为深色卡片。
- 参考 v10.5.76 的命运页结构，在命运结果页主源码隐藏“单宫断事解卦”“双宫断事解卦”和普通“行动建议”。
- 移除命盘深读子模块里的“命运行动建议”，并同步清理命盘子导航与 Debug 必检列表。
- 继续执行“清补丁、归主线”原则：本次没有新增后置脚本、版本抢写、`boot-late / load-late`。
- 已同步更新 `README.md`、`manifest.webmanifest`、`service-worker.js` 缓存名，降低 GitHub Pages / PWA 缓存造成的新旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 在 iPhone Safari 暗夜模式下查看“六宫排盘”，确认命中的留连、速喜、赤口不再是整块亮紫，而是深色卡片加柔和描边。
3. 打开命运结果页详细版，确认不再出现“单宫断事解卦”“双宫断事解卦”和普通“行动建议”。
4. 在命盘子导航里确认没有“建议”子项，命盘深读底部也没有“命运行动建议”卡片。
5. 在 Debug 点“一键自检”，确认没有版本、导航、历史、命盘和暗夜覆盖错误。

### v10.5.109 命盘总览布局与六宫排盘校正版

- 直接在 v10.5.108 命盘字号与六宫间距主源码修复版上修改，继续保持 GitHub 可直接上传的 7 个完整文件。
- 修复命盘“总览”到“长期”之间新专题卡片在 iPhone Safari 下横向撑破的问题：所有命盘深读卡片、专题小卡、胶囊标签和表格都收回到父容器宽度内。
- 修复这些新专题卡片因为宽度溢出导致的“看起来字体异常大、文字被右侧快捷按钮挡住、内容向右跑出屏幕”的问题。
- 修复六宫排盘错乱：新版卡片式排盘不再混用旧六边形定位类，避免大安、速喜、空亡等宫位被旧坐标偏移。
- 补充主源码级 CSS 防线：即使旧 hex 定位类残留，也不会再影响新版六宫排盘卡片。
- 继续执行“清补丁、归主线”原则：本次没有新增后置脚本、版本抢写、`boot-late / load-late`。
- 已同步更新 `README.md`、`manifest.webmanifest`、`service-worker.js` 缓存名，降低 GitHub Pages / PWA 缓存造成的新旧版本跳动。


### v10.5.108 命盘字号与六宫间距主源码修复版

- 直接在 v10.5.107 命盘专题扩展与排盘字号修复版上修改，继续保持 GitHub 可直接上传的 7 个完整文件。
- 修复命盘区域阅读字号异常放大：取消 body、card、detail-only、destiny-deep-card 之间的重复字号叠加。
- 设置里的阅读字号现在用固定主源码字号变量控制，普通/大/超大之间来回切换后，命盘阅读顺序和专题卡片不会残留大字。
- 命盘六宫卡片中“地支/时辰文字”改为标题左侧小胶囊，右上序号独立占位，进一步拉开距离，避免在 iPhone Safari 下贴近或重叠。
- 新增 Debug 字号一致性快照与自检，方便确认命盘深读、命盘六宫和当前 body 字号状态。
- 继续执行“清补丁、归主线”原则：本次没有新增后置脚本、版本抢写、`boot-late / load-late`。
- 已同步更新 `README.md`、`manifest.webmanifest`、`service-worker.js` 缓存名，降低 GitHub Pages / PWA 缓存造成的新旧版本跳动。


## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 切换设置里的阅读字号，确认首页、结果页、历史、分析、弹窗和命盘内容都会跟随变化。
3. 起一课“命运”，打开详细版，检查命盘新增的总览、财运、家宅、人际、成果、长期倾向模块。
4. 查看“六宫排盘”，确认它是卡片式排盘，不再是中心环形图。
5. 查看命盘六宫卡片，确认右上序号不再和地支/时辰文字挤在一起。
6. 切换暗夜模式，确认新增命盘专题和排盘卡片没有浅色残留。
7. 在 Debug 点“一键自检”，确认没有版本、导航、历史、命盘和暗夜覆盖错误。

## v10.5.107 命盘专题扩展与排盘字号修复版

- 命盘专题继续扩展：新增“命盘总览摘要 / 财运与资源参考 / 父母家宅文书参考 / 人际兄弟竞争参考 / 子女子孙成果输出参考 / 长期人生倾向总结”。
- 六宫排盘恢复为更接近 v10.5.76 的卡片式排盘，不再使用手机端占位较大的中心环形布局。
- 初步修复阅读字号和命盘六宫卡片右上序号安全留白。

## v10.5.106 命盘专题内容主源码恢复版

- 参考 v10.5.76 的命盘健康、婚姻与事业模块，把详细专题内容直接整合进 renderDestinyBoard 主源码，不再通过后置脚本插入。
- 命盘健康恢复为“体质与健康参考”：包含自身长期体质、官鬼健康隐患、子孙恢复力、六神表现、五行身体对应表和生活调养提醒。
- 命盘婚姻恢复为更详细的“婚姻与伴侣参考”：按男命/女命/未指定性别侧重，结合妻财、官鬼、父母、兄弟、子孙、掌诀优势挑战和生活提醒。
- 命盘事业恢复为“事业与财富参考”：按官鬼看事业规则、妻财看收入资源、父母看平台文书、子孙看输出成果，并加入行业五行、优势挑战与事业知识库速览。
- 继续保持首页/结果页分离、暗夜模式和历史记录主源码修复成果；本次不新增后置补丁、不恢复 boot-late/load-late。

## v10.5.105 首页结果页分离主源码版

- 参考 v10.5.76 稳定逻辑，恢复占卜结果页与主页输入区独立显示。
- 起课完成或从历史记录点“查看”后，进入结果页状态，只显示本次结果，不再混入场景、问事、起课和版本说明等主页模块。
- 点击右侧快捷按钮“主页”或“重占”后，会退出结果页状态、隐藏结果卡片、恢复首页输入区，并回到顶部。
- 新增结果页分离 CSS 主源码覆盖与 Debug 自检。
- 继续保持 1 个主 style + 1 个主 script，不新增后置补丁。

## v10.5.104 暗夜历史修复主源码版

- 继续参考 v10.5.76 稳定暗夜模式，补齐历史弹窗头部、工具栏、完整备份折叠区、历史列表容器等最后浅色残留。
- 修复非命运历史记录回看兼容：缺少 details、metrics、distribution、rule 或 scene 信息的旧记录会自动补齐后再渲染。
- 历史列表操作归入事件委托主逻辑，查看、收藏、复盘、删除统一由历史主源码处理，减少移动端点击兼容问题。
- 保持 1 个主 style + 1 个主 script，不新增后置补丁、不使用反色滤镜。

## v10.5.103 暗夜残留主源码清理版

- 直接在 v10.5.102 暗夜模式主源码修复版上修改，继续保持 GitHub 可直接上传的 7 个完整文件。
- 继续参考 v10.5.76 的稳定深色主题，不回退整站，只把暗夜残留浅色组件收回主 CSS。
- 补齐暗夜模式下仍会发白/发灰的组件：A/B 选择面板、开启 A/B 按钮、A/B 结果卡片、设置表单、版本说明、规则说明、日期控件、历史/分析表格、模板工具区、命盘来源面板、六亲选项、命盘公式说明、六宫图节点等。
- 优化暗夜模式文字层级：标题、正文、说明文字、折叠标题、输入框、select、checkbox、date/time 控件在暗夜下统一深色底和高对比文字，避免浅紫文字压在白底上看不清。
- Debug 自检新增“暗夜主题关键覆盖检查”：即使当前不在暗夜模式，也会检查主 CSS 是否包含关键暗夜覆盖选择器，避免视觉残留但自检通过的问题再次出现。
- `applyReadingSettings()` 同步更新 `theme-color`，暗夜时浏览器/PWA 主题色也跟随深色主题。
- 继续执行“清补丁、归主线”原则：本次修复写入主 CSS 与主函数，没有新增后置脚本、版本抢写、`boot-late / load-late`。
- 已同步更新 `README.md`、`manifest.webmanifest`、`service-worker.js` 缓存名，降低 GitHub Pages / PWA 缓存造成的新旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次；iPhone Safari 可在地址栏刷新后再打开 Debug。
2. 切换暗夜模式，重点看首页“问事模板 / 清空事项 / A/B 选择起课 / 开启 A/B 对比 / 选择起课方式”这些区域，不应再出现大面积浅色底。
3. 打开历史、分析、设置、场景规则、问题模板、命盘来源设置，确认弹窗内部按钮、表格、输入框和折叠块都是深色主题。
4. 在 Debug 点“一键自检”，确认新增的“暗夜主题关键覆盖检查通过”。
5. 再起一课和从历史查看一课，确认简洁/详细、展开/折叠、导航和快捷按钮没有被这次暗夜修复影响。

# 小六壬占卜 · v10.5.102 暗夜模式主源码修复版

## v10.5.102 暗夜模式主源码修复版

- 直接在 v10.5.101 清补丁与阅读模式修复版上修改，继续保持 GitHub 可直接上传的 7 个完整文件。
- 参考你提供的 v10.5.76 稳定暗夜模式，把黑夜模式恢复为 CSS 变量深色主题，不使用 `invert()` 反色、不使用图片/画布滤镜反转。
- 修复暗夜模式下局部白块/浅色卡片问题：答案摘要、快速导读、术语解释、历史工具区、完整备份、命盘说明、单宫/双宫/三象卡片、Debug 弹窗都纳入主源码深色选择器。
- 修复 Debug 自检误报 `DESTINY_TEMPLATE_MISSING_BOARD`：命运结果在“简洁版”下会隐藏 detail-only 命盘模块，这是正常阅读模式；现在自检改为检查命盘模块是否存在并有内容，不再要求它在简洁版里可见。
- 迁移完成后自动清理旧 `xlr_user_settings_v9_1`、`xlr_view_mode_v9_1`、`xlr_saved_choice_v9_1` 三个旧设置键，减少 `LEGACY_STORAGE_KEYS` 残留警告。
- 继续执行“清补丁、归主线”原则：本次修复写入主 CSS 与主函数，没有新增后置脚本、版本抢写、`boot-late / load-late`。
- 已同步更新 `README.md`、`manifest.webmanifest`、`service-worker.js` 缓存名，降低 GitHub Pages / PWA 缓存造成的新旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次；iPhone Safari 可在地址栏刷新后再打开 Debug。
2. 切换暗夜模式，确认不是反色调：白色卡片不应大面积出现，答案摘要、历史弹窗、导读/术语、命盘和 Debug 都应为深色底。
3. 打开设置，把“默认阅读模式”选成“简洁版”，保存后重新起课或从历史点“查看”，确认结果页默认进入简洁版。
4. 在命运结果页的简洁版状态打开 Debug 一键自检，确认不再误报 `DESTINY_TEMPLATE_MISSING_BOARD`。
5. Debug 自检中旧 `v9_1` 设置键应自动消失；如果浏览器仍保留其他旧键，可再点一次“清理旧缓存”。

# 小六壬占卜 · v10.5.101 清补丁与阅读模式修复版

## v10.5.101 清补丁与阅读模式修复版

- 直接在 v10.5.100 命盘模块主源码归一版上修改，继续保持 GitHub 可直接上传的 7 个完整文件。
- 修复“默认阅读模式=简洁版”不生效：新结果页和历史查看会优先按设置里的默认值打开，不再被旧 `xiaoliuren_view_mode_source = detail` 抢回详细版。
- 修复 Debug 命盘边界误报：回到主页后，隐藏的上一课命盘 DOM 不再被判断成“普通问事页误显示命盘模块”。
- 恢复稳定暗夜模式配色：采用显式深色变量和组件级覆盖，不使用反色滤镜；卡片、按钮、输入框、命盘、历史、分析、弹窗和术语卡片统一深色显示。
- 继续清理旧补丁残留：Debug 不再扫描主脚本/样式的纯文本补丁关键词，避免诊断模块自身和版本记录造成误报；自检更聚焦真实 DOM、window 旧函数和 localStorage 旧键。
- 已同步更新 `README.md`、`manifest.webmanifest`、`service-worker.js` 缓存名，降低 GitHub Pages / PWA 缓存造成的新旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 打开设置，把“默认阅读模式”选成“简洁版”，保存后重新起课或从历史点“查看”，确认结果页默认进入简洁版。
3. 切换暗夜模式，确认不是反色调，卡片、按钮、输入框、命盘、历史、分析和弹窗颜色都稳定。
4. 从命运历史记录查看一课后，点“主页”回到首页，再打开 Debug 自检，确认不再误报普通页串命盘。
5. 在 Debug 里点“清理旧缓存”，可以安全移除已经迁移的旧 `v9_1` 设置键。

## v10.5.100 命盘模块主源码归一版

- 直接在 v10.5.99 设置主键归主线与缓存清理增强版上修改，继续保持 GitHub 可直接上传的 7 个完整文件。
- 本版继续执行“补丁回源码计划”：不新增后置脚本、不做版本抢写、不加入 `boot-late / load-late`，继续保持 `index.html` 内 **1 个主 `<style>` + 1 个主 `<script>`**。
- 新增 `window.XLRDestinyCore` 主命名空间，统一管理命盘状态构建、性别侧重、六亲排法、地支六神、五星、健康婚姻与长期倾向摘要。
- `renderDestinyBoard()` 改为读取 `XLRDestinyCore.buildState()` 的统一结果，减少命盘渲染函数中散落的推导逻辑，后续调试更容易定位。
- Debug 一键自检新增命盘主命名空间、命盘 DOM、命运页必备模块、普通问事误显示命盘、命运/非命运模板边界检查。
- 继续保留 v10.5.99 的设置 source 主键迁移、设置未保存提醒、旧缓存安全清理成果。
- 历史记录、历史分析、复盘统计、导航随动、命运子导航、快捷按钮、阅读模式继续保留前序主源码恢复成果。
- 已同步更新 `README.md`、`manifest.webmanifest`、`service-worker.js` 缓存名，降低 GitHub Pages / PWA 缓存造成的新旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 打开首页，确认标题显示 `v10.5.100 命盘模块主源码归一版`。
3. 用“命运”场景起一课，确认命盘模块正常显示六亲、地支、六神、五星、事业、财务、婚姻、健康、情绪、行动建议。
4. 在命运页打开 Debug，点“一键自检”，重点看是否出现“命盘主命名空间检查通过”“命盘状态构建与模板边界检查通过”。
5. 切回普通问事场景起课，确认普通问事结果页不会显示命盘模块，也不会串用命运模板。
6. 切换命运页“性别侧重”和“六亲排法”，确认命盘内容会刷新，术语解释仍能区分命运/普通问事。
7. 再次测试历史、分析、快捷按钮、阅读模式、设置未保存提醒和 Debug 清理旧缓存。

## v10.5.99 设置主键归主线与缓存清理增强版

- 清理设置类旧主键：用户设置、上次选择、结果简洁/详细视图从旧 `v9_1` 主键迁移到 `source` 主键。
- 保留一次性兼容迁移：老浏览器里已有的 `xlr_user_settings_v9_1`、`xlr_saved_choice_v9_1`、`xlr_view_mode_v9_1` 会自动复制到新的 source 主键；之后运行只读写 source 主键。
- 强化设置未保存提醒：打开设置时立即建立设置快照，不再依赖延迟计时器，关闭设置时能更稳定识别“改了但没保存”的状态。
- Debug 一键自检新增 source 设置主键检查；旧 `v9_1` 设置键被列入旧缓存清理候选，确认迁移后可通过 Debug 安全清理。

## v10.5.98 历史分析与复盘主源码恢复版

- 恢复历史分析与复盘统计主源码：统计卡片、场景分布、起课方式分布、六宫出现频率、末象落点分布、识别类型分布、复盘准确度分布、已复盘场景分布、历史明细表、复盘明细表。
- 新增 `window.XLRAnalyticsCore` 主命名空间，统一管理历史分析打开、关闭、渲染、指标计算、复盘行统计和复盘导出入口。
- 修复复盘统计只看最新 `review` 的问题：分析页会统计完整 `reviews` 时间线，多次复盘不会漏算。

## 当前源码结构

- `index.html`：单一主样式、单一主脚本。
- `window.XLRNavCore`：源码内建导航主模块，负责主导航、命盘子导航、导航刷新、跳转与自动居中。
- `window.XLRHistoryCore`：源码内建历史记录主模块，负责历史保存、读取、规范化、收藏、刷新、导入导出支撑。
- `window.XLRAnalyticsCore`：源码内建历史分析主模块，负责分析渲染、指标计算、复盘时间线统计与导出入口。
- `window.XLRDestinyCore`：源码内建命盘主模块，负责命盘状态构建、六亲/六神/五星摘要、DOM 状态与模板边界自检。
- `window.XLRDebugCore`：源码内建 Debug 主模块，只读状态、复制/保存日志、一键自检、旧缓存安全清理。
- `initSourceUiModules()`：阅读模式、UI 显示、快捷按钮自定义、Debug 显示开关。
- `migrateCoreSettingsStorage()`：设置类主键迁移函数，把旧 `v9_1` 设置迁移到 `source` 主键。
- `bindDestinySourcePanel()`：命运场景的性别侧重与六亲排法选择。
- `renderDestinyBoard()`：源码主函数读取 `XLRDestinyCore.buildState()` 后渲染命盘六亲、地支、六神、五星、婚姻、健康与说明。
