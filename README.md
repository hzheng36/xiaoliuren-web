# 小六壬占卜 · v10.5.15 设置即时保存修正版

基于 v10.5.13 结果模板状态隔离修正版继续更新。

## v10.5.15 设置即时保存修正版

- 保留 Debug 诊断能力，但默认关闭，避免日常使用时干扰页面。
- 在「设置」中新增「开启 Debug 诊断面板」开关。
- 开启后右下角显示「DEBUG」按钮，可打开诊断页面。
- Debug 诊断页面新增「关闭」按钮，同时保留刷新日志、复制日志、清空日志。
- 继续保留 v10.5.13 的命运/非命运结果模板状态隔离修复。
- 更新页面版本记录、README、manifest、service-worker 缓存名，避免旧版本回跳。

## v10.5.13 结果模板状态隔离修正版

- 根据 Debug 日志修复命运/非命运历史切换时的 currentResult 与 result-destiny 状态不同步问题。
- 非命运结果强制隐藏六宫排盘和命运六亲参考盘。
- 普通问事继续显示快速导读、判断重心、综合判断、三象、雷达、行动建议、术语解释。
- 更新 service worker 缓存名，避免旧补丁回跳。
