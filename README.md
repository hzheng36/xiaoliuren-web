# 小六壬占卜 · v10.5.16 Debug左下与导航居中版

基于 v10.5.15 设置即时保存修正版继续小步修复。

## v10.5.16 Debug左下与导航居中版

- Debug 浮动按钮移到左下角，避免和右侧快捷按钮/滚动按钮混在一起。
- 底部章节导航改为以屏幕为基准居中显示，小屏继续支持横向滑动。
- 结果页顶部/底部操作按钮统一居中，减少偏左偏右的视觉问题。
- 保留设置即时保存修复：Debug 开关、阅读模式、背景等设置改动后立即保存。
- 保留命运/非命运结果模板隔离修复。
- 已同步更新 manifest 与 service-worker 缓存名，避免旧版本跳回。
