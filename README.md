### v10.5.127 三步行动方案增强版

本版基于 v10.5.126 现实验证清单增强版继续小步升级。重点仍然不打乱原计划、不插入复盘编辑、不提前接入新的 JSON 知识库，而是继续把普通问事结果页从“知道该核实什么”推进到“知道先做什么、再看什么、最后如何收口”。

1. 在“答案摘要”中新增“三步行动方案”，把结果拆成“先做 / 再看 / 收口”三段。
2. 健康、天气、失物、消息、感情、婚姻、财运、求职/考试、选择、合作、出行、家宅、寻人、贵人、阶段运势等场景会生成不同的三步行动。
3. 三步行动方案会结合末象自动补收口提醒：空亡重证据，赤口重沟通规则，留连重等待反复，速喜重持续性，大安重稳定，小吉重小结果。
4. 保留 v10.5.126 的“落地检查清单”，也保留 v10.5.124 的“接下来观察什么 / 可以怎么追问”和 v10.5.125 的场景深化直接答案。
5. 保持导航双模式、历史记录、复盘、命盘六亲参考盘、结果页顶部复位、暗夜主题和缓存主结构稳定。
6. 阅读字号大小疑似失效的问题已记录，仍按你的要求先放入统一修复清单，不打断当前正常升级节奏。
7. Debug 发版体检表新增“三步行动方案可用”，一键自检通过时会显示“三步行动方案检查通过”。
8. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 用健康场景测试“感冒什么时候好？”，确认答案摘要里出现“三步行动方案”，并按“先记录症状 → 再看 24-48 小时变化 → 最后按医生建议收口”的逻辑显示。
3. 用天气场景测试，确认三步行动会提醒临近天气预报、雷达、云雨持续性和出行影响。
4. 用失物场景测试，确认三步行动会提示最后一次位置、常放位置、包袋夹层、抽屉角落、旧盒旧物等排查顺序。
5. 用合作/合同场景测试，确认三步行动会提示合作范围、金额、责任、交付时间、验收标准和退出机制。
6. 用选择场景测试，确认三步行动会提醒已确认条件、未确认条件、最大风险和小步试行。
7. 测试末象为空亡、赤口、留连、速喜时，三步行动最后一句是否会自动强调证据、沟通规则、等待反复或观察持续性。
8. 测试详细版 / 简洁版，确认新增卡片在两种模式下都不影响折叠模块和导航。
9. 测试设置里的“内容自适应导航 / 固定指针式导航”，确认导航双模式没有被影响。
10. 测试命运结果页，确认命盘六宫、二级导航、婚姻/健康/事业等子模块仍正常。
11. 在 Debug 点“一键自检”，确认版本为 v10.5.127，并看到“白话答案质量检查通过”“追问复盘建议检查通过”“场景深化白话答案检查通过”“现实验证清单检查通过”和“三步行动方案检查通过”。
12. 观察 recentErrors 是否为空，releaseChecklist 是否全部通过。

### v10.5.126 现实验证清单增强版

本版基于 v10.5.125 白话答案场景深化版继续小步升级。重点仍然不打乱原计划、不插入复盘编辑、不提前接入新的 JSON 知识库，而是继续把普通问事结果页从“更会说人话”推进到“更方便现实验证”。

1. 在“答案摘要”中新增“落地检查清单”，把卦象转成用户下一步可以核实的现实项目。
2. 健康、天气、失物、消息、感情、婚姻、财运、求职/考试、选择、合作、出行、家宅等场景会生成不同检查项。
3. 检查清单会结合末象自动变化：空亡重核实证据，赤口重沟通规则，留连重反复等待，速喜重后续持续性，大安重稳定，小吉重先落小结果。
4. 保留 v10.5.124 的“接下来观察什么”和“可以怎么追问”，也保留 v10.5.125 的场景深化直接答案。
5. 保持导航双模式、历史记录、复盘、命盘六亲参考盘、结果页顶部复位、暗夜主题和缓存主结构稳定。
6. 阅读字号大小疑似失效的问题已记录，仍按你的要求先放入统一修复清单，不打断当前正常升级节奏。
7. Debug 发版体检表新增“现实验证清单可用”，一键自检通过时会显示“现实验证清单检查通过”。
8. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 用健康场景测试“感冒什么时候好？”，确认答案摘要里出现“落地检查清单”，并提示体温、精神状态、饮食睡眠、症状变化等现实观察点。
3. 用天气场景测试，确认检查清单会提醒临近预报、雷达、实际云雨和出行准备。
4. 用失物场景测试，确认会提示最后使用处、常放位置、包袋夹层、抽屉角落、旧物旧盒等排查路径。
5. 用合作/合同场景测试，确认会提示合作范围、金额、责任、交付时间、验收标准和退出机制。
6. 用出行、家宅、选择、财运等场景测试，确认检查项会按场景变化，不退回通用话术。
7. 测试末象为空亡、赤口、留连、速喜时，检查清单最后一条是否会自动强调核实、沟通规则、预留时间或观察持续性。
8. 测试详细版 / 简洁版，确认新增清单在两种模式下都不影响折叠模块和导航。
9. 测试设置里的“内容自适应导航 / 固定指针式导航”，确认导航双模式没有被影响。
10. 测试命运结果页，确认命盘六宫、二级导航、婚姻/健康/事业等子模块仍正常。
11. 在 Debug 点“一键自检”，确认版本为 v10.5.126，并看到“白话答案质量检查通过”“追问复盘建议检查通过”“场景深化白话答案检查通过”和“现实验证清单检查通过”。
12. 观察 recentErrors 是否为空，releaseChecklist 是否全部通过。

### v10.5.125 白话答案场景深化版

本版基于 v10.5.124 追问建议与复盘引导版继续小步升级。重点不打乱原计划、不插入复盘编辑、不提前接入新的 JSON 知识库，而是继续把普通问事结果页的“答案摘要”做得更像日常对话。

1. 新增 v10.5.125 场景深化层：选择决策、合作合同、出行、家宅、寻人、贵人、阶段运势等场景有更贴近现实的直接答案。
2. “直接答案”继续采用“先说结论 → 再讲三象过程 → 最后给下一步”的结构，减少模板味。
3. “为什么这么看”改成更自然的过程解释：本象是起点，中象是过程，末象是落点，重点解释为什么末象最值得重视。
4. “你现在该怎么做”补强更多场景的行动建议：合作看条款责任，出行看路线证件天气，家宅看合同维修隐患，寻人看可靠线索，选择看信息是否足够。
5. 保留 v10.5.124 的“接下来观察什么”和“可以怎么追问”，继续方便后续复盘和追问。
6. 保持导航双模式、历史记录、复盘、命盘六亲参考盘、结果页顶部复位、暗夜主题和缓存主结构稳定。
7. Debug 发版体检表新增“场景深化白话答案可用”，一键自检通过时会显示“场景深化白话答案检查通过”。
8. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 用选择场景测试“我该不该现在做决定？”，确认直接答案会围绕信息是否足够、能否小步试行来讲。
3. 用合作/合同场景测试，确认答案会提醒条款、责任、交付时间和白纸黑字。
4. 用出行场景测试，确认答案会提醒路线、证件、天气、备用方案和延误。
5. 用家宅场景测试，确认答案会提醒维修、合同、邻里沟通、照片证据和安全隐患。
6. 用寻人/贵人/阶段运势测试，确认不会退回过于通用的话术。
7. 测试 v10.5.124 已有的“接下来观察什么”和“可以怎么追问”，确认没有丢失。
8. 测试详细版 / 简洁版，确认折叠模块显示正常，普通问事不串命盘模板。
9. 测试设置里的“内容自适应导航 / 固定指针式导航”，确认导航双模式没有被影响。
10. 测试命运结果页，确认命盘六宫、二级导航、婚姻/健康/事业等子模块仍正常。
11. 在 Debug 点“一键自检”，确认版本为 v10.5.125，并看到“白话答案质量检查通过”“追问复盘建议检查通过”和“场景深化白话答案检查通过”。
12. 观察 recentErrors 是否为空，releaseChecklist 是否全部通过。

### v10.5.124 追问建议与复盘引导版

本版基于 v10.5.123 白话答案二次增强版继续小步升级。重点仍然不动导航、历史、命盘、缓存和页面结构，而是在“答案摘要”里增加更适合后续使用的复盘与追问引导。

1. 新增“接下来观察什么”：把结果转成现实观察点，方便用户在之后验证这课到底准不准、卡在哪里。
2. 新增“可以怎么追问”：按场景给出下一课可问方向，让一次占问自然延伸到更具体、更可操作的问题。
3. 追问建议会结合场景与末象自动变化：末象空亡强调核实证据，赤口强调沟通规则，留连强调拖延反复，速喜强调快消息能否持续。
4. 补强财运、婚姻等场景的直接答案匹配，减少退回通用话术的概率。
5. 保留 v10.5.123 的问题意图识别、重复句压缩、三象过程说明和行动建议去重。
6. 保持导航双模式、历史记录、命盘六亲参考盘、结果页顶部复位、暗夜主题和缓存主结构不变。
7. Debug 发版体检表新增“追问建议与复盘引导可用”，一键自检通过时会显示“追问复盘建议检查通过”。
8. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 用健康场景测试“感冒什么时候好？”，确认答案摘要底部出现“接下来观察什么”和“可以怎么追问”。
3. 用天气场景测试“今天会不会下雨？”，确认观察点会提醒临近复核天气预报和实际云雨变化。
4. 用失物场景测试“我的钥匙在哪里？”，确认观察点会提示最后一次使用处、收纳处、包袋夹层等现实寻找顺序。
5. 用感情/婚姻/消息场景测试，确认追问建议更贴合对方态度、实际行动和沟通结果。
6. 用财运场景测试，确认直接答案和追问建议会围绕到账、合同、风险和是否落实，而不是走通用话术。
7. 测试详细版 / 简洁版，确认折叠模块显示正常，普通问事不串命盘模板。
8. 测试设置里的“内容自适应导航 / 固定指针式导航”，确认导航双模式没有被影响。
9. 测试命运结果页，确认命盘六宫、二级导航、婚姻/健康/事业等子模块仍正常。
10. 在 Debug 点“一键自检”，确认版本为 v10.5.124，并看到“白话答案质量检查通过”和“追问复盘建议检查通过”。
11. 观察 recentErrors 是否为空，releaseChecklist 是否全部通过。

### v10.5.123 白话答案二次增强版

本版基于 v10.5.122 专项解读白话串联版继续小步升级。重点仍然不动导航、历史、命盘、缓存和页面结构，而是继续提高普通问事“答案摘要”的日常对话感。

1. 新增问题意图识别：系统会先判断用户是在问“什么时候”“能不能”“对方态度”“位置线索”“下一步怎么做”等，再用对应口吻组织直接答案。
2. “直接答案”更贴近真人解释：健康、天气、失物、消息、感情、求职/考试等高频场景会少用模板句，多用具体场景话术。
3. 减少重复句：当本象和中象同宫时，会合并成“本象、中象同落某宫”的表达，避免连续重复“有消息、有机会”等句子。
4. “为什么这么看”改成三象过程说明：先讲本象、中象、末象各自看什么，再讲它们如何串成一个过程。
5. 行动建议继续保留专业提醒，但会做去重和压缩，并按末象补充最该注意的地方。
6. Debug 发版体检表新增“白话答案二次增强可用”，一键自检通过时会显示“白话答案质量检查通过”。
7. 保留 v10.5.122 的专项解读串联、v10.5.121 的场景白话、v10.5.120 的发版体检、旧补丁审计、导航双模式、命运/非命运边界和结果页顶部复位逻辑。
8. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 用健康场景测试“感冒什么时候好？”，确认直接答案会按“问时间”回答，而不是泛泛说“有消息、有机会”。
3. 用天气场景测试“今天会不会下雨？”，确认直接答案会按雨象、阵雨、落空、稳定变化来讲。
4. 用失物场景测试“我的钥匙在哪里？”，确认直接答案会给出现实寻找线索。
5. 用感情或消息场景测试“对方会不会主动联系我？”，确认直接答案会强调实际行动和明确回复。
6. 测试详细版 / 简洁版，确认折叠模块显示正常，普通问事不串命盘模板。
7. 测试设置里的“内容自适应导航 / 固定指针式导航”，确认导航双模式没有被影响。
8. 测试命运结果页，确认命盘六宫、二级导航、婚姻/健康/事业等子模块仍正常。
9. 在 Debug 点“一键自检”，确认版本为 v10.5.123，并看到“白话答案质量检查通过”。
10. 观察 recentErrors 是否为空，releaseChecklist 是否全部通过。

### v10.5.122 专项解读白话串联版

本版基于 v10.5.121 场景白话增强版继续小步升级。重点仍然不动导航、历史、命盘、缓存和页面结构，而是继续把普通问事结果页的“问事专项解读”改得更像日常对话。

1. 升级“问事专项解读”：从关键词堆叠改成“这一段看什么 → 落宫怎么说 → 回到你的问题怎么用”的结构。
2. 新增场景化“下一步核实建议”，健康、天气、消息、求职/考试、感情、失物等场景会给出更贴近日常操作的提醒。
3. 三象解读和双宫断事继续接入场景专属白话，避免退回通用“有消息、有机会”等泛化话术。
4. 健康场景会继续把速喜、留连、赤口、空亡转译为症状变化、恢复反复、急性不适和复查提醒。
5. Debug 发版体检表新增“专项解读串联可用”，一键自检通过时会显示“专项解读白话串联检查通过”。
6. 保留 v10.5.121 的场景白话、v10.5.120 的发版体检、旧补丁审计、导航双模式、命运/非命运边界和结果页顶部复位逻辑。
7. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 用普通问事测试“健康”，问题可用“感冒什么时候好？”，重点看“问事专项解读”是否按“这一段看什么 / 落宫怎么说 / 回到你的问题 / 下一步怎么核实”展示。
3. 测试“天气”和“消息等待”，确认专项解读能把落宫转成云雨、阵雨、落空、回复快慢、消息是否算数等日常表达。
4. 测试“详细版 / 简洁版”，确认折叠模块显示正常，普通问事不串命盘模板。
5. 测试设置里的“内容自适应导航 / 固定指针式导航”，确认导航双模式没有被影响。
6. 测试命运结果页，确认命盘六宫、二级导航、婚姻/健康/事业等子模块仍正常。
7. 在 Debug 点“一键自检”，确认版本为 v10.5.122，并看到“场景白话覆盖检查通过”和“专项解读白话串联检查通过”。
8. 观察 recentErrors 是否为空，releaseChecklist 是否全部通过。

### v10.5.121 场景白话增强与健康专项版

本版基于 v10.5.120 稳定收口与补丁审计版继续小步升级。重点不动导航、历史、命盘、缓存和页面结构，而是增强普通问事结果页的“场景化白话表达”，尤其避免健康、天气、消息、求职/考试等场景继续套用过于通用的话术。

1. 新增场景化宫位白话层：健康、天气、消息、求职/考试等场景会把大安、留连、速喜、赤口、小吉、空亡转译成更贴近该场景的表达。
2. 健康类重点优化：速喜不再泛泛显示“有消息、有机会”，而是转成“症状变化较快、可能短期缓解或突然明显”；留连转成“恢复拖延反复”；赤口转成“急性不适/炎症/疼痛类提醒”；空亡转成“症状判断不明朗、必要时复查”。
3. 答案摘要的“为什么这么看”改为按本象/中象/末象的角色与场景解释，减少普通话术串场。
4. 普通问事行动建议改为“本象/中象/末象 + 场景白话”，减少重复句和泛泛而谈。
5. Debug 发版体检表新增“场景白话覆盖可用”，一键自检通过时会显示“场景白话覆盖检查通过”。
6. 保留 v10.5.120 的发版体检表、旧补丁审计、导航双模式、命运/非命运边界和结果页顶部复位逻辑。
7. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 用普通问事测试“健康”，问题可用“感冒什么时候好？”，确认答案摘要和行动建议更像健康建议，而不是通用“有消息、有机会”。
3. 测试“天气”，确认天气场景仍能把速喜/赤口/空亡转成云雨、阵雨、落空等天气表达。
4. 测试设置里的“内容自适应导航 / 固定指针式导航”，确认上一版导航双模式没有被影响。
5. 测试命运结果页，确认命盘六宫、二级导航、婚姻/健康/事业等子模块仍正常。
6. 在 Debug 点“一键自检”，确认版本为 v10.5.121，并看到“场景白话覆盖检查通过”。
7. 观察 recentErrors 是否为空，releaseChecklist 是否全部通过。

### v10.5.120 稳定收口与补丁审计版

本版基于 v10.5.119 当前稳定状态继续收口。重点不是新增大功能，而是把已经确认好用的导航双模式、命运/非命运边界、结果页复位、历史和 Debug 自检统一固定下来，作为后续稳定基准候选。

1. 保留两种导航样式，并在设置中使用正式名称：`内容自适应导航` 与 `固定指针式导航`。
2. 导航设置继续写入 `xiaoliuren_user_settings_source`，兼容旧值 `fixed`，新保存时使用 `fixed-pointer` / `adaptive` 语义，避免旧设置丢失。
3. Debug 新增“发版体检表”快照，统一检查 title、meta、首页标题、版本记录、源码结构、重复 ID、重复函数、运行错误、导航主源码、命运/非命运边界和旧缓存键。
4. Debug 新增旧补丁审计信息：严格项只检查实际 DOM / class / data 中是否仍有旧版本锁、boot-late、load-late 等痕迹；`patch`、`fallback`、`setTimeout`、`escapeHtml` 等只作为人工审计参考，避免误报正常主源码。
5. 继续保持普通问事不显示“六宫排盘”和“命运六亲参考盘”；命运场景继续保留六宫排盘、命运六亲参考盘和命盘二级导航。
6. 修正 Debug 里少量旧提示文字，把所有主源码来源升级为 `mainline-v10.5.120`。
7. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 打开设置，确认导航样式可在“内容自适应导航”和“固定指针式导航”之间切换。
3. 分别在首页、普通问事结果页、命运结果页测试两种导航样式，确认一级/二级导航都能正常高亮和横向跟随。
4. 打开普通问事结果页，确认不显示“六宫排盘”和“命运六亲参考盘”。
5. 打开命运结果页，确认“六宫排盘”“命运六亲参考盘”和命盘二级导航仍然存在。
6. 在 Debug 点“一键自检”，确认版本为 v10.5.120，并查看 `releaseChecklist` / `sourceAudit`。
7. 观察 recentErrors 是否为空，确认没有 JS 运行错误。

### v10.5.119 双层导航间距再压缩版

本版基于 v10.5.118 继续小步微调。你说一级、二级导航之间还可以再缩短一点，所以这版只压缩双层导航之间的距离，不重写导航逻辑，不改变上一版已确认好用的“自适应跟随 / 固定指针式”切换功能。

1. 一级章节导航与命盘二级导航之间的距离再次缩短，双层导航更贴近。
2. 手机底部导航中，二级导航与一级导航之间的视觉间距从约 6px 压缩到约 2px，尽量减少底部空间占用。
3. 顶部导航位置同步压缩间距，避免切换到顶部后仍显得偏松。
4. 桌面/大屏底部双层导航同步微调，保持两层导航不重叠、不跳动。
5. 保留 v10.5.118 的导航模式可切换设置：自适应跟随和固定指针式都继续可用。
6. Debug 主源码版本、导航 adaptive 标记、自检来源同步升级到 v10.5.119。
7. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 打开命运结果页，观察一级导航和二级导航之间的距离是否比 v10.5.118 更贴近。
3. 分别测试“自适应跟随”和“固定指针式”，确认两种模式下都没有重叠。
4. 把导航位置切到顶部/底部各测试一次，确认间距都更紧凑但仍可点击。
5. 滚动到命盘、事业、财运、婚姻、健康等模块，确认二级导航高亮和横向跟随仍然正常。
6. 在 Debug 点“一键自检”，确认没有运行错误，并看到版本为 v10.5.119。

### v10.5.118 一级二级导航间距微调版

本版基于 v10.5.117 继续微调导航体验。你说一级和二级导航之间的距离可以再缩短一点点，所以这版只做小步改动：压缩双层导航之间的上下间距，让手机底部或顶部的导航占用更少空间，同时不改变上一版已经确认好用的“导航模式可切换”功能。

1. 一级章节导航与命盘二级导航之间的距离缩短，双层导航更贴近、更紧凑。
2. 底部导航模式和顶部导航模式同步调整，避免只修一个方向导致体验不一致。
3. 保留 v10.5.117 的“导航跟随模式”设置：仍可在“自适应跟随”和“固定指针式”之间切换。
4. 保留自适应导航的起点优先、出界轻微跟随和真实 active 高亮逻辑。
5. 保留固定指针式导航的中槽体验，当前模块仍会自动滚到中间指针位置。
6. Debug 主源码版本、导航 adaptive 标记、自检来源同步升级到 v10.5.118。
7. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 打开命运结果页，观察一级导航和二级导航之间的距离是否比 v10.5.117 略微缩短。
3. 在设置里分别切换“自适应跟随”和“固定指针式”，确认两种模式下间距都正常。
4. 把导航位置切到顶部/底部各测试一次，确认双层导航不会重叠，也不会离得太远。
5. 滚动到命盘、事业、财运、婚姻、健康等模块，确认二级导航高亮和横向跟随仍然正常。
6. 在 Debug 点“一键自检”，确认没有运行错误，并看到版本为 v10.5.118。

### v10.5.117 导航模式可切换设置版

本版基于 v10.5.116 继续优化导航体验。你提到“当前自适应导航不错，但和之前固定指针式导航有点背道而驰，两个都喜欢”，所以这版把两种体验都保留下来，并放进设置里让用户自己选择。

1. 阅读设置里新增“导航跟随模式”。
   - 自适应跟随：沿用 v10.5.116，导航底板按内容收缩，默认从第一个按钮开始，当前项快出界时才轻微跟随。
   - 固定指针式：恢复旧版中槽体验，导航底板保持固定宽度，当前模块自动滚到中间指针位置。
2. 一级导航和命盘二级导航同时支持两种模式，不再只能选一种体验。
3. 导航模式保存到 `xiaoliuren_user_settings_source`，不新增旧式补丁 key，不走后置抢写。
4. 保存设置后会立即刷新导航样式和滚动逻辑，无需重新打开页面。
5. Debug 增加 `navMode`、`navFollowMode`、`navModeSwitch` 字段，并在一键自检中加入“导航模式设置切换检查通过”。
6. 保留 v10.5.116 的起点优先、自适应 reveal 和高亮跟随修复；固定指针式则单独走居中滚动逻辑。
7. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 打开设置，找到“导航跟随模式”。
3. 选择“自适应跟随”，确认导航底板会根据内容宽度收缩，进入结果页时从第一个按钮开始。
4. 选择“固定指针式”，确认一级导航和命盘二级导航会恢复类似旧版的中间指针式体验，当前模块会自动滚到中间。
5. 在命运结果页滚动到“命运六亲参考盘”，测试二级导航两种模式是否都生效。
6. 在 Debug 点“一键自检”，确认没有运行错误，并看到导航模式相关字段。

### v10.5.116 导航起点与高亮跟随修正版

本版基于 v10.5.115 继续修复自适应导航。你这次截图里的问题是：导航项较多、宽度受限时，上一版为了让 active 靠中，会自动把导航条横向滚到中间，导致一级/二级导航看起来不是从第一个按钮开始。

1. 一级导航和命盘二级导航改成“起点优先”：进入结果页、进入命盘区域、刷新或重新渲染时，默认先从第一个按钮开始显示。
2. 横向跟随逻辑从“强制居中”改为“出界轻微跟随”：当前 active 按钮仍在可视范围内时，导航条不再自动移动；只有 active 快出界时，才轻微滚动到可见范围。
3. 绿色高亮 `slot-active` 不再按导航中线选按钮，而是始终跟随真实 active 按钮，避免出现“结果 active，但答案变绿”这类错位。
4. 继续保留 v10.5.115 的自适应宽度：按钮少时底板变短，按钮多时仍然横向滑动。
5. Debug 导航状态升级到 `beta-v10.5.116`，可以继续观察 `scrollLeft`、`overflow`、`currentTarget`、`active`、`slotActive` 是否一致。
6. 同步更新首页版本、版本内容更新、README、manifest 和 service-worker 缓存名，避免旧版本跳动。
7. 继续保持 7 个完整文件交付，无后置补丁、无延迟抢写补丁。

## 这版重点测试

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 打开命运结果页，确认一级导航默认从“结果”开始显示。
3. 滚动到“命运六亲参考盘”区域，确认二级导航默认从“起盘 / 六宫 / 总览...”顺序开始，不再一打开就跳到中间。
4. 向下滚动到后面的事业、财运、婚姻、健康等模块，确认二级导航只在当前项快出界时轻微横向移动。
5. 观察绿色高亮：它应该始终落在真实当前模块按钮上，不再因为导航中线位置而错位。
6. 在 Debug 点“一键自检”，确认没有运行错误，并确认导航状态为 `beta-v10.5.116`。

### v10.5.115 一级二级导航自适应 Beta 版

本版基于 v10.5.114 继续做导航体验 Beta 调整，重点把一级章节导航和命盘二级导航改为“内容自适应宽度”，减少手机端底部导航和绿色高亮区域占用空间。

1. 一级章节导航改为自适应宽度：导航项较少时只包住按钮，不再固定撑满整屏。
2. 命盘二级导航同步改为自适应宽度：子项少时更紧凑，子项多时仍保留横向滑动。
3. 优化 active / slot-active 高亮逻辑：没有横向溢出时绿色高亮优先跟随当前 active 按钮；有横向溢出时继续支持自动居中。
4. 优化“主页”快捷按钮：返回首页时改为立即滚动归零，并追加多次校准，减少 iPhone Safari 回首页停在中部的问题。
5. Debug 日志新增导航自适应 Beta 状态字段，可看到 `adaptive`、`overflow`、`cssWidth`、`cssMaxWidth` 等信息。
6. 一键自检新增“导航自适应 Beta 检查通过”，继续保留导航目标、旧补丁残留、结果页顶部复位、命运/非命运模板边界等检查。
7. 继续保持 7 个完整文件交付，无后置补丁、无延迟抢写补丁。
