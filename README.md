# 小六壬占卜 · v10.5.218 可见问题修复版 2 / 阶段稳定封版

## v10.5.218 可见问题修复版 2 / 阶段稳定封版

这一版作为阶段收尾后的“可见问题修复版 2 / 阶段稳定封版”。本版不改起课算法、不改六宫断法、不新增大功能，只做最后一轮肉眼可见问题修复、Debug 非阻断项整理、版本缓存统一和全站稳定性体检。

### 本版修复与整理

1. Debug 阶段封版状态
   - Debug 顶部保留 v10.5.217 的短说明 + 完整说明折叠；
   - 新增“阶段稳定封版”短提示；
   - 明确当前版本边界：只整理显示、缓存、非阻断项、版本信息和发版体检，不改核心占卜逻辑。

2. 弹窗底部安全留白继续收口
   - 补强 Debug、历史记录、历史分析、设置页、复盘弹窗等滚动区域的底部安全留白；
   - 继续减少 iPhone / iPad / Safari 底部导航遮挡内容的问题；
   - 移动端按钮最小触控高度继续统一。

3. 非阻断项与发版体检整理
   - 保留 Debug 黑名单 / 白名单机制；
   - 把 v10.5.217 的 Debug 折叠说明和历史分析高亮修复纳入继承检查；
   - 新增 v10.5.218 阶段封版体检。

4. 版本与缓存统一
   - 页面标题、首页版本、meta、版本记录、README、manifest、service-worker 缓存名已统一到 v10.5.218；
   - 当前主线 source 已统一到 `mainline-v10.5.218`；
   - 旧 `mainline-v10.5.217` 不再作为当前主线 source 残留。

### 本版新增体检

- 新增 `xlr218StageSealAudit`；
- 检查阶段封版提示是否存在；
- 检查 v10.5.217 可见修复是否继承；
- 检查旧主线 source 是否清空；
- 检查弹窗底部安全留白、暗夜模式和移动端样式是否存在；
- 已加入 Debug release checklist。

### 本版检查

- 已检查主脚本语法。
- 已检查 service-worker 语法。
- 已检查 manifest JSON。
- 已检查静态 HTML 重复 ID。
- 已检查重复 function 定义。
- 已检查当前 source 统一到 `mainline-v10.5.218`。
- 已检查旧 `mainline-v10.5.217` 不再残留为当前主线 source。
- 已检查 ZIP 完整性。

### 后续建议

这一版可以作为阶段稳定封版。后续建议先实际测试几轮，不急着继续加功能；如果还有明显问题，再开 v10.5.219 只做“阶段封版后小修补”，否则可以暂时停在 v10.5.218。
