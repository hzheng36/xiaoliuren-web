# 小六壬占卜 · v10.5.85 源码内建Debug恢复版

## v10.5.85 源码内建Debug恢复版

- 继续执行“补丁回源码计划”：不新增后置补丁，不用版本抢写，不用多阶段延迟覆盖。
- 在 v10.5.84 的单一主脚本、单一主样式基础上，先把 **Debug 诊断** 作为源码主模块写回。
- Debug 现在只读取页面状态并复制日志，不改变占卜逻辑、结果模板、导航状态或阅读设置。
- 已继续检查旧 Debug 残留：不再使用 v10514/v10546/v10574/v10581/v10582 这类后置诊断按钮和保存日志脚本。
- 页面版本、首页标题、版本内容更新、README、manifest、service-worker 缓存名已统一到 v10.5.85。

## 当前源码结构

- `index.html`：1 个主 `<style>`，1 个主 `<script>`。
- Debug 是主脚本内部的 `initSourceDebug()` 模块，不是独立脚本块。
- 后续恢复功能时继续按主函数/主模块写回，不再靠末尾追加补丁。

## 后续恢复顺序建议

1. 阅读模式与字号设置主模块化。
2. 快捷按钮自定义与保存主模块化。
3. 命运导读、术语解释、导航子模块主模块化。
4. 历史记录、复盘、分析与导出稳定性复查。

## v10.5.84 源码净化过渡版

- 继续以 **v10.5.76 稳定版** 为回溯基准，但这一版先不急着恢复功能。
- 本版重点是“继续清”：移除旧后置运行逻辑、旧诊断、保存日志、重复版本写入、延迟覆盖、版本抢写等残留。
- 诊断等功能现在缺失是预期现象：先把运行残留清掉，后续再把 v10.5.76 中确认要保留的功能按源码主模块方式重写回来。
- 当前版本统一为：页面 title、首页标题、版本内容更新、README、manifest、service-worker 缓存名全部指向 v10.5.84。
- 后续原则：不再新增后置运行脚本；需要恢复功能时，重写主函数、主模块、主渲染流程。

## 后续恢复清单

- 基础：起课、结果渲染、历史记录、设置、阅读模式、快捷按钮。
- 学习：快速导读、术语解释、口诀、三象、单宫/双宫断事。
- 命运：命盘、健康、婚姻、事业子模块与对应导航。
- 诊断：重新设计源码内建 诊断，不再照搬旧后置诊断脚本。

## v10.5.83 源码整合清旧后置逻辑版

- 以 **v10.5.76 稳定版** 为唯一干净基线，正式启动“旧后置逻辑回源码计划”。
- 清理后置 诊断、增强诊断、版本锁写、旧保存日志按钮、重复版本记录、延迟启动/延迟加载 类延迟覆盖残留。
- 统一当前版本源：页面 title、首页标题、版本内容更新、README、manifest、service-worker 缓存名全部改为 v10.5.83。
- 版本内容更新由源码主流程统一输出，同一版本只显示一次，避免主页版本信息新旧跳动。
- 保留 v10.5.76 已验证稳定的命运导读电脑端布局、导航、阅读字号、快捷按钮、折叠与设置主逻辑。
- 后续发版原则：**不再新增后置运行脚本**；需要修复时优先重写主函数、主模块或主渲染流程，把稳定逻辑直接整合回源码。

## v10.5.76 命运导读电脑端布局修正版

- 修复命运板块“快速导读”在电脑端被嵌套 Grid 挤成竖排文字、右侧出现大面积空白的问题。
- 增加宽屏专用导读布局规则，四张导读卡片在电脑端按正常横向卡片显示。
- 作为 v10.5.83 的稳定回溯基线。

## v10.5.74 诊断诊断增强版

- 升级 诊断 诊断系统，复制日志和保存日志都会输出增强版诊断数据。
- 新增设置状态检测：阅读字号、默认阅读模式、章节导航开关、导航位置、详细模块默认折叠、记住场景/起课方式等。
- 新增导航检测：一级/二级导航按钮、active/slot-active 状态、跳转锁剩余时间、目标模块可见性和位置。
- 新增快捷按钮检测：当前浮动按钮、设置页勾选状态、快捷按钮新旧本地存储字段。
- 新增折叠显示审计、版本记录去重审计、本地存储摘要、性能/视口信息、最近操作事件和最近错误记录。
- 诊断 面板新增“保存增强日志”按钮，可直接下载完整 JSON；仍保留手动刷新、复制日志、清空日志。

## v10.5.73 版本记录整理补齐版

- 修复“版本内容更新”板块中当前版本重复出现、随后又跳回 v7–v8 的顺序错乱问题。
- 将版本内容更新改为统一清单输出，同一版本只显示一次，不再被旧版本记录脚本反复追加。
- 补齐 v10.5.41 至 v10.5.72 之间的主要更新记录，并按新到旧顺序整理。
- 早期版本统一放到后半段：v10.5.22–v10.5.36、v10.4、v10.3、v10.1–v10.2、v10.0、v9、v8、v7。
- 同步更新页面标题、README、manifest、service-worker 缓存名，继续检查旧版本跳动问题。

## v10.5.72 导航点击防抖稳定修正版

- 修复点击章节卡点导航后内容画面上下抖动的问题。
- 将章节导航点击改为 window 捕获优先处理，先拦截旧 document 点击监听，再执行单次稳定跳转。
- 跳转锁定期间暂停旧 MutationObserver / 旧导航刷新 / 旧口诀术语刷新，避免页面滚动位置被二次抢回。
- 为结果页阅读区关闭滚动锚定，减少 iPhone Safari 地址栏和模块高度重算带来的回弹。
- 保留 v10.5.71 快捷按钮勾选直存、v10.5.69 阅读字号下拉选择、章节导航开关和顶部/底部导航位置设置。

## v10.5.71 快捷按钮勾选直存修正版

- 修复“快捷按钮设置”勾选新增按钮后，退出设置又恢复修改前状态的问题。
- 快捷按钮勾选改为点击/输入/关闭设置时直接读取当前可见勾选状态并写入最终配置。
- 同步写入 v1071/v1070/v9919 字段，避免旧自动保存流程抢回旧配置。
- 不改阅读字号、导航核心和折叠逻辑，保留 v10.5.69 后已稳定功能。



## v10.5.69 阅读字号下拉选择版

- 阅读文字大小从数字输入框改为 13px–24px 下拉选择。
- 继续保留 v10.5.68 的 px 字号变量方案，结果页正文、答案、三象、术语、建议等模块继续按用户选择的字号显示。
- 设置页自身不跟随阅读字号变化，避免切换字号时控件抖动。
- 保留 v10.5.66 的性能回退、v10.5.64 的快捷按钮设置修复，以及此前导航/折叠/记住设置等稳定修复。

## v10.5.68 自定义阅读字号输入版

- 阅读文字大小改为 13px–24px 自定义数字输入，不再使用“偏小 / 标准 / 偏大 / 更大”分段按钮。
- 设置页控件本身不再跟着阅读字号变大变小，避免选择时按钮和布局抖动。
- 结果页正文、答案摘要、快速导读、判断重心、综合判断、三象、术语、行动建议等模块直接使用 px 字号变量。
- 保留 v10.5.66 的性能回退、v10.5.64 的快捷按钮设置修复，以及此前导航/折叠/记住设置等稳定修复。


## v10.5.67 阅读字号分段按钮修正版

- 修复“阅读文字大小”选择偏大/更大后仍恢复为“标准”的问题。
- 将阅读文字大小从容易被旧自动保存流程读错的原生下拉，改为轻量分段按钮：偏小 / 标准 / 偏大 / 更大。
- 点击字号按钮后立即写入独立意图键，并同步旧字号键，旧自动保存流程不能再把非标准字号覆盖回标准。
- 不使用 v10.5.65 那种重型全页面扫描或 MutationObserver，保持 v10.5.66 的流畅度。
- 保留快捷按钮设置、默认阅读模式、记住场景/起课方式、章节导航开关、顶部/底部导航和命运答案导航修复。


## v10.5.66 性能回退与阅读字号轻量修正版

- 修复“阅读文字大小”无论选择偏小、偏大或更大，退出设置后都会回到“标准”的问题。
- 修复“快捷按钮设置”勾选新增按钮后，退出设置又自动恢复到修改前的问题。
- 新增最终设置出口：阅读字号和快捷按钮配置以设置表单与用户意图为准，旧自动保存流程不得覆盖。
- 保留 v10.5.63 的命运答案导航、高亮判定、章节导航开关、默认阅读模式和记住场景/起课方式等修复。

## v10.5.63 阅读字号最终生效修正版

- 修复“阅读文字大小”被旧自动保存流程和移动端字号旧后置逻辑覆盖，导致设置后正文大小不变的问题。
- 新增阅读字号独立意图锁定，偏小 / 标准 / 偏大 / 更大可即时生效并持久保存。
- 阅读字号现在直接作用于占卜结果正文、答案摘要、三象、单宫、双宫、象意、雷达、行动建议和术语解释。
- 保留 v10.5.62 已修复的默认阅读模式、记住场景/起课方式和快捷按钮设置。



## v10.5.63 阅读字号最终生效修正版

- 统一设置保存链路，修复多个设置项被旧自动保存流程覆盖的问题。
- 修复“阅读文字大小”不生效：结果页正文、答案卡、三象、术语和建议会随设置变化。
- 修复“默认阅读模式”不生效：选择简洁版/详细版后，会同步应用到当前结果视图和后续新起课结果。
- 修复“记住上次选择的场景 / 起课方式”不生效：切换后会即时写入本地，下次打开自动恢复。
- 修复“快捷按钮设置”不生效：勾选、取消勾选和排序都会独立保存，退出设置后不再复原。
- 保留 v10.5.61 的命运答案导航与高亮判定修复、v10.5.60 的章节导航开关持久锁定、v10.5.57 的详细模块默认折叠。

## v10.5.61 命运答案导航与高亮判定修正版

- 修复命运结果页主导航缺少「答案」的问题。
- 命运主导航顺序更新为：结果 → 导读 → 答案 → 重心 → 命盘 → 综合 → 口诀 → 三象 → 雷达 → 术语 → 建议。
- 修复滚动停在「答案摘要」模块时，底部导航仍显示/高亮为「导读」的问题。
- 优化固定指针式导航的当前章节判定线，更贴近手机屏幕阅读中心。
- 保留 v10.5.60 的章节导航开关持久锁定、v10.5.58 的顶部/底部导航位置、v10.5.57 的详细模块默认折叠。

## v10.5.60 章节导航开关持久锁定修正版

- 修复设置中关闭「显示章节卡点导航」后，退出设置或自动保存时又被马上重新打开的问题。
- 新增 `showSectionNav` 独立意图锁定，用户选择关闭/开启后，旧设置写入也不能把它覆盖回默认值。
- 关闭后同时隐藏一级章节导航和命运二级导航；开启后恢复固定指针式导航和旧版胶囊外观。
- 保留 v10.5.58 的导航位置顶部/底部设置、v10.5.57 的详细模块默认折叠，以及 v10.5.56 的命运导航顺序与点击防抖。


## v10.5.59 章节导航显示开关修正版

- 修复设置中「显示章节卡点导航」关闭后不生效的问题。
- 关闭后会同时隐藏一级章节导航和命运二级导航；重新开启后恢复固定指针式导航。
- 修复固定指针导航样式里的 `display:flex!important` 覆盖旧 `hide-section-nav` 隐藏规则的问题。
- 增加 UI 设置持久化保护，避免旧自动保存流程遗漏 `showSectionNav` 后把导航重新显示。
- 保留 v10.5.58 的导航位置持久化、v10.5.57 的详细模块默认折叠和 v10.5.56 的命运导航顺序与点击防抖逻辑。


## v10.5.58 导航位置持久化修正版

- 修复「详细模块默认折叠」勾选后，非命运结果页详细模块仍默认展开的问题。
- 设置中新增「导航位置」选项，可选择底部或顶部显示。
- 底部模式保持：一级导航在下面、二级导航在上面；顶部模式反向显示：一级导航在上面、二级导航在下面。
- 顶部导航模式下，点击跳转会预留导航高度，避免目标标题被导航遮住。
- 保持 v10.5.56 的命运导航顺序、综合入口、固定指针式导航和点击防抖逻辑。


## v10.5.56 命运导航顺序与点击防抖修正版

- 继续以 v10.5.46 内容基线为准，不沿用 v10.5.47-v10.5.49 中导致内容缺失的导航改动。
- 修复 v10.5.51 中“导航不会随着内容滚动而横向同步”的问题：页面上下滚动时，当前模块 chip 会自动滑入固定选中槽。
- 保留固定选中槽 / 指针式导航：选中槽固定在导航中间，导航横向轨道跟随当前模块移动。
- 避免旧问题复发：导航横向同步只改变导航条 scrollLeft，不反向触发页面上下滚动，不写入 hash，不做二次纠偏。
- 用户手动横向滑动导航时会短暂暂停自动同步，避免“刚滑动就被抢回去”。
- 命运主导航保留“导读”，命运二级导航仍只保留命盘内部子模块：命盘 / 健康 / 婚姻 / 事业。

## v10.5.51 导航显示稳定修正版

- 继续以 v10.5.46 内容基线为准，不沿用 v10.5.47-v10.5.49 中导致内容缺失的导航改动。
- 固定选中槽改为导航背景层，不再使用伪元素，避免 iPhone Safari 中出现横线错位、选中槽跑到页面上方的问题。
- 解绑旧 v9.9.6 主导航 scroll / resize / load 监听，防止旧 renderSectionJumpNav 继续重画导航。
- 停用旧 v10.5.4 命运二级导航 updater，由 v10.5.51 导航核心统一控制。
- 固定槽只显示 slot-active，不再让页面滚动产生另一个大面积 active 高亮，避免一个导航同时出现两个高亮。
- 命运二级导航只在命盘大模块范围内显示，进入口诀、三象、雷达、术语后自动隐藏。
- 主导航与二级导航点击只执行单次 auto scroll，不写 hash，不二次纠偏，不横向抢居中。

## v10.5.50 固定选中槽导航重构版

- 以 v10.5.45 为基线整合为完整 7 文件 GitHub 替换包，不再只提供热修复 JS/CSS。
- 统一结果页状态源：currentResult、body class、结果卡片显示状态和标题文字共同判断，避免命运页被误判为普通页。
- 修复旧旧后置逻辑延时 setVersion / lockVersion 把首页标题、meta 或 诊断 版本抢回 v10.5.44 的问题。
- 命运结果页导航固定补回“导读”，并稳定为“结果 → 导读 → 重心 → 命盘 → 口诀 → 三象 → 雷达 → 术语 → 建议”。
- 术语解释统一为学习型内容：命运与非命运各显示一个术语模块，折叠按钮和右侧快捷按钮都能真实隐藏/展开内容。
- 点击导航改为单次稳定跳转，跳转期间加锁，减少点击命盘后再点术语/建议时的连续抖动。
- 诊断 增加 v10.5.50 审计：状态源、导航目标、折叠内容区、旧模块残留和最近交互记录。
- 同步更新页面版本记录、README、manifest、service-worker，并保留原 3 个图标文件。

## v10.5.45 导读回归与术语学习增强版

- 修复非命运“术语解释”点击折叠后内容仍显示的问题，统一折叠状态与内容隐藏规则。
- 命运结果页恢复“快速导读”，并保留导航里的“导读”入口。
- 普通术语与命运术语改为学习型内容，合并旧第二个术语解释重点，并扩展三象串联、六宫掌诀、六亲、六神、地支、五星、生克和现实校验说明。
- 命运与非命运只显示各自一个术语解释模块，避免两个术语互相抢显示或快捷按钮控制错对象。
- 诊断 增加 v10.5.45 审计信息：结果页判断、命运判断、术语块数量、导航目标和折叠内容区状态。
- 同步更新页面版本记录、README、manifest、service-worker，并继续检查旧版本跳动问题。

## v10.5.44 导读术语去重与诊断增强版

- 补齐命运结果页导航里的“导读”，顺序调整为“结果 → 导读 → 答案 → 重心 → 命盘 → 综合 → 三象 → 口诀 → 雷达 → 术语”。
- 停用旧 v10.3.3 动态 details 导读/术语，避免页面底部出现两个“术语解释”。
- 把主导读、行动建议、普通术语、命运术语统一纳入 `collapsible-block`，右侧快捷“展开 / 折叠”按当前场景统一控制。
- 诊断 系统新增重复模块检查、折叠审计、导航目标审计、最近快捷按钮事件，并新增“保存详细日志”。
- 同步更新页面版本记录、README、manifest、service-worker，并继续检查旧版本跳动问题。

## v10.5.42 主页结果状态隔离修复版

- 修复返回主页后仍残留 `result-page`、`result-destiny`、旧结果 class、旧 hash 和 `currentResult`，导致主页导航被结果页导航覆盖的问题。
- 返回主页、重新占卜、启动清理都会同步隐藏结果卡片、清空当前结果，并重建主页导航。
- 主导航判断改为“结果页 class + 结果卡片可见”同时成立才进入结果模式，避免历史结果残留让主页误判。
- 右侧快捷按钮“展开 / 折叠”按当前页面状态执行：结果页控制结果模块，主页控制主页说明/版本 details。
- 同步更新页面版本记录、README、manifest 与 service-worker 缓存名，并继续防止旧版本标题回跳。


## v10.5.41 源码整合稳定版

- 以 v10.5.36 导航术语与跳转稳定版为干净基线，不继续沿用 v10.5.37-v10.5.40 的命运术语后置旧后置逻辑。
- 删除末尾动态创建命运术语模块的后置脚本，把“命运术语解释”直接整合到结果页主 DOM 结构。
- 主阅读导航正式加入命运“术语”目标，位置在“雷达”之后；右侧“一键展开 / 一键折叠”可以控制该模块。
- 主导航点击改为单次稳定定位，避免 smooth scroll、导航居中和折叠刷新叠加导致画面抖动。
- 同步更新页面版本记录、README、manifest 与 service-worker 缓存名，并检查旧版本标题回跳问题。


## v10.5.36 导航术语与跳转稳定版

- 命运结果页主阅读导航补回“术语解释”，位置在“雷达”之后，方便直接跳到术语说明。
- 新增命运专用术语解释块，解释三象、掌诀、六亲、地支五行、六神、五星和生克关系，不影响普通问事模板。
- 修复点击“命盘”之后的主导航目标时画面连续抖动的问题：拦截旧旧后置逻辑叠加的多次 smooth scroll，改为单次稳定定位。
- 跳转期间临时关闭滚动动画和导航自身平滑滚动，避免 Safari 上主导航、二级导航、折叠块刷新同时触发造成三次位移。
- 保留 v10.5.35 主题回切显色修复，并同步更新页面版本记录、README、manifest 与 service-worker。

## v10.5.35 主题回切显色修复版

- 修复白天/夜间快速切换后 `html[data-theme]` 仍停留在 dark，导致白天模式下命运口诀、说明卡片等模块继续显示深色的问题。
- 将主题判断来源统一为 `body` 当前的 `reading-theme-night / reading-theme-day`，不再把 `html` 旧 class 当作判断依据，避免暗夜状态自我锁死。
- 增加白天模式显色兜底覆盖：命运口诀卡、术语/导读卡、内容说明卡在白天模式下强制恢复浅色背景和正常文字颜色。
- 保留 v10.5.34 的主题快速切换优化，不恢复全量白块扫描，不新增滚动轮询。
- 同步更新页面版本记录、README、manifest 与 service-worker 缓存名，并检查脚本语法和 7 个部署文件。

## v10.5.34 主题切换性能修复版

- 修复白天/夜间切换时卡顿：切换期间临时禁用全页面 transition、animation 和 backdrop-filter。
- 暗夜白块兜底扫描改为避开主题切换窗口，并加入节流与数量上限，减少命运长页面全量重扫造成的 1 秒左右卡顿。
- 白天/夜间快捷按钮只更新主题 class 与按钮文字，不触发结果页重渲染、导航重建或版本标题重写。
- 保留 v10.5.33 已确认修好的命运主导航与二级导航顺序，不改排盘逻辑和事业/健康/婚姻子模块内容。
- 同步更新页面版本记录、README、manifest 与 service-worker 缓存名，并检查旧旧后置逻辑/版本跳动残留。

## v10.5.33 口诀与综合导航顺序修复版

- 修复命运结果页滚到“综合判断”区域时，主导航误高亮“口诀”的问题。
- 将命运“本课用到的口诀”模块重新固定到“三象解读”之后，让页面实际 DOM 顺序与底部主导航顺序一致。
- 保留 v10.5.32 的命运二级导航主从同步逻辑：命盘 / 健康 / 婚姻 / 事业只在命盘子模块范围内显示。
- 不新增轮询，不改排盘逻辑，不改事业、健康、婚姻子模块内容，避免再次引起页面抖动。
- 同步更新页面版本记录、manifest 与 service-worker 缓存名，并重新检查旧旧后置逻辑抢回旧版本问题。

## v10.5.32 命运二级导航主从同步修复版

- 继续修复命运二级导航显示范围：二级导航现在会跟主阅读导航同步。
- 当主导航当前阅读位置已经是“口诀、综合、三象、雷达”等主模块时，命盘 / 健康 / 婚姻 / 事业二级导航会强制隐藏。
- 二级导航只在主导航仍处于“命盘”范围，并且屏幕阅读点确实落在命盘、健康、婚姻或事业子模块内时显示。
- 保留 v10.5.30 / v10.5.31 的稳定方案，不新增轮询，不改排盘逻辑，不改主导航居中逻辑，降低再次抖动风险。
- 同步更新页面版本记录、manifest 与 service-worker 缓存名，并重新检查部署路径。

## v10.5.31 命运二级导航范围修复版

- 修复命运二级导航在离开“命运六亲参考盘”子模块后仍然停留的问题。
- 二级导航现在只在命盘、健康、婚姻、事业这几个命运子模块范围内显示；滚到“本课用到的口诀 / 综合判断 / 三象 / 雷达”等主模块后自动隐藏。
- 保留 v10.5.30 已验证稳定的导航去重方案：事业不再进入主阅读导航，只保留在命运二级导航中。
- 本次不修改主导航居中、页面滚动、诊断、快捷按钮和排盘逻辑，避免再次引入页面抖动。
- 同步更新页面版本记录、manifest 与 service-worker 缓存名，并重新检查部署路径。

## v10.5.30 稳定版导航去重修复版

- 基于 v10.5.26 稳定代码回退修复，不沿用 v10.5.29 的新导航锁定逻辑。
- 仅修复“事业”同时出现在底部主导航和命运二级导航的问题。
- 事业保留在“命运六亲参考盘”的二级导航中，与健康、婚姻并排。
- 禁止 v10.5.23 旧旧后置逻辑继续把事业注入底部主阅读导航。
- 保留 v10.5.26 已验证稳定的页面滚动、诊断、快捷按钮、宽表横向滑动与版本显示逻辑。

- 修复 v10.5.25 中页面标题、首页说明和脚本内部版本仍可能被旧旧后置逻辑改回旧版本的问题。
- 清理/停用会造成手机端卡顿的旧旧后置逻辑轮询：事业子导航 0.9 秒轮询、宽表 1.8 秒轮询、事业模块 2.2 秒轮询、快捷按钮 1.5 秒版本轮询。
- 诊断 改为按需刷新/复制日志，不再开启后持续写入大量 interval/mutation 日志；保留设置开关、关闭按钮、复制日志和保存日志能力。
- 保留事业与财富参考正式模块、命运二级导航“命盘 / 健康 / 婚姻 / 事业”、宽表横向滑动、右侧快捷按钮稳定和当前导航自动居中。
- 同步更新页面版本记录、manifest 与 service-worker 缓存名，并重新检查路径和内嵌脚本语法。

## v10.5.25 事业子导航正式版

- 命运场景“事业与财富参考”去掉 Beta 标记，作为正式命盘子模块保留。
- 事业模块纳入“命运六亲参考盘”二级导航，与“健康”“婚姻”并排显示。
- 二级导航会在命盘内部滚动范围内自动出现，并随当前阅读位置高亮“命盘 / 健康 / 婚姻 / 事业”。
- 保留 v10.5.24 宽表横向滑动修复、右侧快捷按钮稳定修复、诊断 保存日志与当前导航自动居中逻辑。
- 同步更新页面版本记录、README、manifest 与 service-worker 缓存名。

## v10.5.24 口诀表横向滑动版

- 命运场景中的婚姻、健康、事业等口诀/知识库宽表统一改为横向滑动。
- 修复手机端表格列被压缩，导致文字竖排、内容难读的问题。
- 为宽表增加“此表可左右滑动查看更多内容”提示，交互参考“六亲读盘补充说明”。
- 保留 v10.5.23 事业模块、右侧快捷按钮稳定修复、诊断 保存日志和导航居中逻辑。


基于 v10.5.22 快捷按钮内置修复版继续小步扩展。

## v10.5.23 事业模块版
- 命运结果页新增“事业与财富参考”子模块。
- 事业模块沿用健康、婚姻命盘子模块的成功结构，只读取已有命盘信息，不改动排盘逻辑。
- 模块基于六亲、掌诀、地支、五行、六神、五星与生克关系，输出事业趋势、财富趋势、适合行业、工作方式、优势挑战、组合建议与行动提醒。
- 底部/浮动阅读导航新增“事业”入口，方便在命运结果页快速跳转到事业子模块。
- 增加移动端宽度、换行和表格横向滚动保护，避免正文被右侧快捷按钮区域裁切。
- 同步更新页面版本记录、README、manifest 与 service-worker 缓存名。

## v10.5.22 快捷按钮内置修复版
- 修复右侧“白天/夜间”按钮仍由后续阅读模式脚本单独插入，刷新时会突然出现并造成右侧快捷区跳动的问题。
- 将“白天/夜间”正式加入 v9.9.19 统一快捷按钮系统，与“主页 / 重占 / 电脑 / 展开 / 折叠 / 置顶 / 到底”等按钮一起生成。
- 快捷按钮设置中新增“白天/夜间”，可统一开关与排序；旧快捷配置缺少该按钮时，会按默认位置插入到“折叠”和“置顶”之间。
- 修正旧阅读模式旧后置逻辑的 `addToggleButton()` 检测逻辑，避免它在已有内置按钮时再额外创建一个白天/夜间按钮。
- 保留 v10.5.21 对命运婚姻子模块移动端换行、首页说明稳定和缓存更新的修复。

## v10.5.21 移动端稳定修复版
- 修复命运结果页“婚姻与伴侣参考”子模块在手机端文字被右侧区域裁切、无法完整显示的问题。
- 强化婚姻子模块的移动端宽度约束、换行、表格横向滚动和右侧安全留白。
- 统一首页版本说明文案，避免旧旧后置逻辑刷新后改写 hero 文案造成标黄区域跳动。
- 白天/夜间快捷按钮提前参与快捷区渲染，减少刷新时右侧按钮后出现造成的抖动。
- 诊断 增加 `__v10521MobileStability` 标记，可用于确认本旧后置逻辑已加载。

## v10.5.20 导航与命运口诀修复版
- 修复历史结果页导航偶尔混入首页导航“顶部 / 场景 / 问事 / 起课”的问题，结果页导航现在会按当前结果完整重建。
- 命运结果页强制保留并显示“本课用到的口诀”，导航中同步出现“口诀”。
- 打开历史结果后重新同步 `result-page` / `result-destiny` 状态，减少普通模板和命运模板互相污染。
- 诊断 日志增加 `overall`、`formula`、`formulaContent` 监测项，方便判断口诀模块是否存在、显示和有内容。

## v10.5.58 导航位置持久化修正版

- 修复设置中切换“导航位置：顶部”后，退出设置又恢复到底部的问题。
- 为 `navPosition` 增加持久化保护，避免旧设置自动保存逻辑覆盖新字段。
- 设置打开、切换、关闭、刷新后都会重新读取并应用导航位置。
- 保持 v10.5.57 的详细模块默认折叠修复，以及 v10.5.56 的命运导航顺序和固定指针式导航逻辑。
