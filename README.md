# 小六壬占卜 · v10.5.98 历史分析与复盘主源码恢复版

## v10.5.98 历史分析与复盘主源码恢复版

- 直接在 v10.5.97 历史记录主源码恢复版上修改，继续保持 GitHub 可直接上传的 7 个完整文件。
- 本版重点恢复历史分析与复盘统计主源码：统计卡片、场景分布、起课方式分布、六宫出现频率、末象落点分布、识别类型分布、复盘准确度分布、已复盘场景分布、历史明细表、复盘明细表。
- 新增 `window.XLRAnalyticsCore` 主命名空间，统一管理历史分析打开、关闭、渲染、指标计算、复盘行统计和复盘导出入口。
- 修复复盘统计只看最新 `review` 的问题：现在分析页会统计完整 `reviews` 时间线，多次复盘不会漏算。
- 复盘准确度兼容增强：旧数据里的 `inaccurate` 和当前使用的 `miss` 都会按“不准”处理，减少历史数据口径不一致。
- Debug 一键自检新增历史分析主命名空间、分析 DOM、分析日期控件、复盘导出按钮、分析指标函数和复盘统计函数检查。
- 继续执行“补丁回源码计划”：本版没有新增后置脚本，没有 `boot-late / load-late`，没有版本抢写。
- 当前源码继续保持 `index.html` 内 **1 个主 `<style>` + 1 个主 `<script>`**。
- 已同步更新 `README.md`、`manifest.webmanifest`、`service-worker.js` 缓存名，降低 GitHub Pages / PWA 缓存造成的新旧版本跳动。

## 这版怎么测试历史分析和复盘

1. 上传 7 个文件到 GitHub Pages，并强制刷新一次。
2. 打开 `历史`，任选一条记录添加 2 条以上复盘。
3. 打开 `分析`，确认 `复盘条目` 会显示多条，而不是只显示 1 条。
4. 检查分析页是否出现 `末象落点分布`、`识别类型分布`、`复盘明细表`。
5. 测试分析日期筛选：不选日期统计全部，选某一天只统计当天。
6. 点击 `导出复盘 TXT`，确认可以导出所有复盘时间线。
7. 打开 Debug，点 `一键自检`，重点看是否有：`历史分析主命名空间检查通过`、`历史分析 DOM 检查通过`、`历史分析日期控件检查通过`、`复盘统计函数检查通过`。

## v10.5.97 历史记录主源码恢复版

- 恢复历史记录主源码：保存、查看、收藏/取消收藏、删除、清空、搜索、场景筛选、日期筛选、TXT 导出、历史 JSON 导出/导入、完整备份导出/导入、复盘时间线。
- 新增 `window.XLRHistoryCore` 主命名空间，统一管理历史读取、规范化、写入、收藏、刷新。

## 当前源码结构

- `index.html`：单一主样式、单一主脚本。
- `window.XLRNavCore`：源码内建导航主模块，负责主导航、命盘子导航、导航刷新、跳转与自动居中。
- `window.XLRHistoryCore`：源码内建历史记录主模块，负责历史保存、读取、规范化、收藏、刷新、导入导出支撑。
- `window.XLRAnalyticsCore`：源码内建历史分析主模块，负责分析渲染、指标计算、复盘时间线统计与导出入口。
- `window.XLRDebugCore`：源码内建 Debug 主模块，只读状态、复制/保存日志、一键自检、旧缓存安全清理。
- `initSourceUiModules()`：阅读模式、UI显示、快捷按钮自定义、Debug显示开关。
- `bindDestinySourcePanel()`：命运场景的性别侧重与六亲排法选择。
- `renderDestinyBoard()`：源码主函数直接渲染命盘六亲、地支、六神、五星、婚姻、健康与说明。
