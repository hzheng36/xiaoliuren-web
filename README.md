# 小六壬占卜 · v10.5.8 普通问事运行状态实修版

- 根据运行诊断结果修复：sceneKey=marriage 但页面没有进入普通结果修复状态。
- 非命运场景直接读取 currentResult 实时隐藏六宫排盘，并移除底部“排盘”导航。
- 非命运场景每次刷新“本场景判断重心”，若原函数返回空则使用兜底内容。
- 非命运结果页首次渲染后回到结果卡片顶部。

- 同步更新 manifest 与 service-worker 缓存名。